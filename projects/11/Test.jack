class Test {
   function void main() {
     var Array a; 
     var int length;

     //let a = Array.new(length); 
     let length = Keyboard.readInt("ddd");
     let a = Array.new(length); 

     return;
   }
}